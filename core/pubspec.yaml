name: core
description: Core services, DI, networking, and helpers for monorepo apps.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.8.0+1
  pretty_dio_logger: ^1.4.0
  get_it: ^8.0.3
  shared_preferences: ^2.5.3
  flutter_secure_storage: ^9.2.4
  flutter_dotenv: ^5.2.1
  flutter_bloc: ^9.1.1
  flutter_screenutil: ^5.9.3
  intl: ^0.20.2
  firebase_core: ^3.14.0
